{
  "name": "40swap",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "server-backend",
    "swap-frontend",
    "shared"
  ],
  "engines" : { 
    "node" : ">=22.0.0 <23.0.0"
  }
}
